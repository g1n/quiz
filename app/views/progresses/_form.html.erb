

<%= form_for @progress do |f| %>

  <% if @progress.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@progress.errors.count, "error") %> prohibited
        this progress from being saved:
      </h2>
      <ul>
        <% @progress.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :user, "User ID" %><br>
    <%= f.text_field :user %>
  </p>
 
  <p>
    <%= f.label :level, "User level" %><br>
    <%= f.text_area :level %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>